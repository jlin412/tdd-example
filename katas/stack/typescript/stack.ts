// ════════════════════════════════════════════════════════════════════
// Stack — production skeleton (TypeScript)
// ════════════════════════════════════════════════════════════════════
// The requirement lives in the story, not here:
//   · Classic:  ../StackStory.md
//   · Extended: ../StackExtendedStory.md
//
// TDD rules for this file:
//   · Add NOTHING here until a RED test in stack.spec.ts demands it.
//   · Write the MINIMUM that makes the current red test green.
//   · Refactor only on green (prompts for that live in the spec file).
//
// One thing `npm test` will NOT do for you: type-check. Vitest strips the
// types away without checking them, so run `npm run typecheck` as well.

export class Stack {
  // Intentionally empty. Your first failing test tells you which method to
  // create — and the mob picks the names (STEP 1 in stack.spec.ts uses
  // `isEmpty` only as a placeholder).
}
